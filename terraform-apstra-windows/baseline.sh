#!/usr/bin/bash

yum -y install expect

#cp start.exp <hostname>.exp
#sed -i 's/$$mgmt_ip$$/<host-ip>/g' /root/terraform/<hostname>.exp 
#sed -i 's/$$host_name$$/<hostname>/g' /root/terraform/<hostname>.exp 
#expect /root/terraform/<hostname>.exp &
